/**
 * 後台表格中「資料來源為公版（NUWA 市集）」的欄位標示。
 *
 * 私版的後台會同時列出兩種資料：
 *   - 公版來的（帳號、手機、方案 —— 帳號與付費的真相來源，私版唯讀）
 *   - 私版自己的（學員暱稱、MBTI、課程進度 —— 私版可寫）
 *
 * 兩者混在同一張表裡，管理員很難一眼分辨哪些欄位改了沒用
 * （公版欄位在私版改不動，要去公版後台改）。用底色區隔。
 *
 * ⚠️ 目前是寫死的常數。後續若要做成後台設定頁可調（存 DB），
 *    改的是取值來源，呼叫端不用動 —— 所以呼叫端一律引用這個常數，
 *    不要在 JSX 裡直接寫 bg-blue-50。
 */
export const MARKET_FIELD_HEADER_CLASS = 'bg-blue-50 text-blue-700';
